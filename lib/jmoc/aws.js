// Generated by CoffeeScript 1.9.2
(function() {
  'use strict';
  var aws, config, dynamo, localCreds;

  aws = require('aws-sdk');

  dynamo = require('dynamodb-doc');

  config = require('./config');

  localCreds = new aws.SharedIniFileCredentials({
    profile: config.aws.access.profile
  });

  if (localCreds.accessKeyId != null) {
    aws.config.credentials = localCreds;
  }

  aws.config.apiVersions = {
    dynamodb: config.aws.ddb.version
  };

  aws.config.update({
    region: config.aws.access.region
  });

  if (typeof module !== "undefined" && module !== null) {
    module.exports = {
      ddb: new dynamo.DynamoDB(new aws.DynamoDB())
    };
  }

}).call(this);
